// assign a variable speed that take value from the user
const speed = parseInt( prompt('Enter your speed'));
// use else if statement to check if value taken will be executed in which condition 
if(speed <70){
    // this will take all the values that are less than 70
    console.log('Ok');
}
else if(((speed-70)/5<=1 )){
    // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 70 t0 75
  console.log("Point:1");
}
else if(((speed-70)/5<=2 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 76 t0 80
    console.log("Point:2");
  }
  else if(((speed-70)/5<=3 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 81 t0 85
    console.log("Point:3");
  }
  else if(((speed-70)/5<=4 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 86 t0 90
    console.log("Point:4");
  }
  else if(((speed-70)/5<=5 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 91 t0 95
    console.log("Point:5");
  }
  else if(((speed-70)/5<=6 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 96 t0 100
    console.log("Point:6");
  }
  else if(((speed-70)/5<=7 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 101 t0 105
    console.log("Point:7");
  }
  else if(((speed-70)/5<=8 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 106 t0 110
    console.log("Point:8");
  }
  else if(((speed-70)/5<=9 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 111 t0 115
    console.log("Point:9");
  }
  else if(((speed-70)/5<=10 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 116 t0 120
    console.log("Point:10");
  }
  else if(((speed-70)/5<=11 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 121 t0 125
    console.log("Point:11");
  }
  else if(((speed-70)/5 >=12 )){
     // when adding 5 to 70 will be 75.this condition will take the value and subtract from 70 divide it and test solution if it lies within the stated argument.
    // this will be executed if the values from 126 and above
    console.log("Licence suspended");
  }